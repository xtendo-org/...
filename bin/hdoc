#!/bin/bash
firefox -P Reset $(stack path --snapshot-doc-root)/$(stack ls dependencies | grep "^$1 " | sed -e 's/ /-/')/index.html
# echo $(stack path --snapshot-doc-root)/$(stack ls dependencies | grep "^$1 " | sed -e 's/ /-/')/index.html
